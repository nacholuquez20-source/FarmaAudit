[pytest]
; Los tests de bot escriben `async def test_...` sin @pytest.mark.asyncio
; (ver test_meta_api_audit.py, test_verification_flow.py) — sin este modo,
; pytest-asyncio los ignora silenciosamente ("async def functions are not
; natively supported") en vez de correrlos.
asyncio_mode = auto
